"""# tuple is same as list the main differences are that tuple is immutalbe
means it can't change
also it is done with circular brackets()
circular brackets are also known as parenthesis
"""
tup=("hellow world","how are you","i am fine")
print(tup)
print(len(tup) , type(tup)) # same functions
print(min(tup)) # are
print(max(tup)) # everywhere so this are same
a1 = ("ello ")
print(a1)
